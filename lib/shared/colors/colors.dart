// lib/core/constants/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Cores Primárias
  static const Color primary = Color(0xff319945); // Verde principal
  
  // Cores Secundárias
  static const Color secondary = Color(0xFF8C8C8C); // Cinza

  // Cores Neutras (Texto e Fundo)
  static const Color background = Color.fromARGB(255, 247, 247, 247); // Cinza claro
  static const Color text = Color(0xFF404040); // Cinza muito escuro
  
  
  // Cores Neutras (Texto e Fundo)
  static const Color cardMatematica = Color(0xFF1E74C6); // Cinza claro
  static const Color cardPortugues = Color(0xFFE8A22D); // Cinza muito claro

  // Cores de Feedback (Erros, Sucesso, Alerta)
  static const Color error = Color(0xFFE53935); // Vermelho
  static const Color success = Color(0xFF4CAF50); // Verde
  static const Color warning = Color(0xFFFFA726); // Laranja
}